# Placeholder until the backend is actually deployed (Phase 6 of booking-system-roadmap.md).
# A production mobile build needs a publicly reachable API, which does not exist yet — so this
# value is intentionally wrong rather than absent, to fail loudly if a prod build happens early.
EXPO_PUBLIC_API_BASE_URL=https://api.example-not-deployed-yet.invalid
